@model IEnumerable<LibrarySystem.Models.Transaction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Consumer.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Inventory.BookTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Consumer.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Inventory.BookTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReferrenceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReferrenceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReferrenceID })
        </td>
    </tr>
}

</table>
